# خطوات الإعداد المتبقية

الكود جاهز. الخطوات التالية تحتاج حسابك أنت — نفّذها بالترتيب.

---

## ١. غيّر كلمة مرور قاعدة البيانات 🚨 عاجل

كلمة المرور القديمة مرفوعة في مستودع GitHub عام ضمن commit `46cbb97`
(ملف `init-db.mjs`). حذف الملف لا يكفي — النسخ المخزّنة والـ forks تبقى.

1. افتح [لوحة Supabase](https://supabase.com/dashboard) ← مشروعك
2. `Settings` ← `Database` ← `Reset database password`
3. اختر كلمة مرور جديدة قوية

بعدها راجع `Reports` ← `API` وابحث عن طلبات من عناوين لا تعرفها.

---

## ٢. أنشئ حساب الدخول

النظام الآن محجوب خلف تسجيل الدخول، وتحتاج حساباً واحداً لنفسك.

1. `Authentication` ← `Users` ← `Add user` ← `Create new user`
2. أدخل بريدك وكلمة مرور
3. فعّل **Auto Confirm User** (وإلا سيطلب تأكيد البريد)

### أغلق التسجيل الذاتي (مهم)

حتى لا يستطيع أحد إنشاء حساب لنفسه والدخول لبياناتك:

`Authentication` ← `Sign In / Providers` ← `Email` ← عطّل **Allow new users to sign up**

---

## ٣. أضف المفاتيح إلى GitHub Secrets

البناء على GitHub Pages يحتاجها، وإلا سيفشل.

`Settings` ← `Secrets and variables` ← `Actions` ← `New repository secret`

| الاسم | القيمة |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | الرابط من `Settings ← API` في Supabase |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | مفتاح `publishable` من نفس الصفحة |

> هذان المفتاحان يظهران للزوار داخل ملفات JS — وهذا طبيعي وآمن
> لأن RLS مفعّل الآن. الخطر الحقيقي كان كلمة مرور قاعدة البيانات.

---

## ٤. اجعل المستودع خاصاً (موصى به)

النظام يحتوي أسماء زبائن ومبالغ وديون. حتى مع تفعيل RLS، لا داعي
لأن يكون الكود وتاريخه مكشوفين.

`Settings` ← `General` ← أسفل الصفحة ← `Change repository visibility`

⚠️ GitHub Pages من مستودع خاص يحتاج خطة مدفوعة. إن لم تكن متاحة،
أبقِ المستودع عاماً — البيانات نفسها محمية بـ RLS.

---

## التشغيل محلياً

```bash
npm install
npm run dev
```

الملف `.env.local` موجود على جهازك ومستثنى من git.
عند العمل على جهاز آخر انسخ `.env.example` إليه واملأ القيم.

---

## ما تم إنجازه

- ✅ حُذفت الملفات الحاوية على كلمة مرور قاعدة البيانات
- ✅ أُنشئ جدول `categories` الناقص
- ✅ فُعِّل RLS على الجداول العشرة (كانت كلها مفتوحة للعامة)
- ✅ أُضيف تسجيل الدخول وحُجب النظام خلفه
- ✅ نُقلت المفاتيح إلى متغيرات بيئة
- ✅ صار حفظ المشتريات والصيانة والدفعات يصل لقاعدة البيانات
- ✅ صار خصم المخزون (بيع/تلف/إنتاج) يُحفظ فعلاً
- ✅ صُحِّحت أسماء أعمدة كانت تُرجع `undefined` في المبالغ والكلف
- ✅ أُضيفت معالجة أخطاء لكل عمليات الحفظ
- ✅ صفر أخطاء في TypeScript و ESLint (كانت ٣٧)
